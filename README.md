# Run_LLM_models_locally

In this repo there is complete guide to how to run llm models locally using ollama and LM studio.

Running models locally helps devlopers to use open source llm models for free of cost and run those models in their local machine.

And devlopers can also use those LLM models to their small ai projects

